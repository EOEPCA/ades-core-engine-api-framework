cmake_minimum_required(VERSION 3.5)
project(proc-comm-zoo NONE)

include(ExternalProject)
ExternalProject_Add(proc-comm-lib-argo
        GIT_REPOSITORY    "https://github.com/EOEPCA/proc-comm-zoo.git"
        GIT_TAG           "develop"
        SOURCE_DIR        "${CMAKE_CURRENT_BINARY_DIR}/proc-comm-zoo"
        BINARY_DIR        "${CMAKE_CURRENT_BINARY_DIR}/proc-comm-zoo-build"
        CONFIGURE_COMMAND ""
        BUILD_COMMAND     ""
        INSTALL_COMMAND   ""
        TEST_COMMAND      ""
)


